{% trans_default_domain 'sfs_shop' %}
{% extends '@SfsShop/admin_layout.html.twig' %}
{% block title %}{{ 'admin_orders.update.page_title'|trans }}{% endblock %}

{% block content %}
    <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3">{{ 'admin_orders.update.title'|trans }}</h1>
    </div>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <div class="d-flex justify-content-end align-items-center">
            <a href="{{ url('sfs_shop_admin_orders_list') }}" class="btn btn-outline-secondary ml-3">{{ 'admin_orders.update.actions.cancel.button'|trans }}</a>
            <a href="{{ url('sfs_shop_admin_orders_delete', {'entity':entity}) }}" class="btn btn-danger ml-3">{{ 'admin_orders.update.actions.delete.button'|trans }}</a>
            <input type="submit" value="{{ 'admin_orders.update.actions.update.button'|trans }}" class="btn btn-primary ml-3" />
        </div>
    {{ form_end(form) }}
{% endblock content %}