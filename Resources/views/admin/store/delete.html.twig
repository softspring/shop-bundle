{% trans_default_domain 'sfs_shop' %}
{% extends '@SfsShop/admin_layout.html.twig' %}
{% block title %}{{ 'admin_stores.delete.page_title'|trans }}{% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <div class="d-flex justify-content-end align-items-center">
            <a href="{{ url('sfs_shop_admin_stores_list') }}" class="btn btn-outline-secondary ml-3">{{ 'admin_stores.delete.actions.cancel.button'|trans }}</a>
            <input type="submit" value="{{ 'admin_stores.delete.actions.delete.button'|trans }}" class="btn btn-danger ml-3" />
        </div>
    {{ form_end(form) }}
{% endblock content %}